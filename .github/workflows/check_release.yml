name: Lint
on:
  workflow_call:

jobs:
  check-release:
    runs-on: ubuntu-latest
    steps:
      - name: Check branch and tag
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        run: exit 1
